import {getCustomers,getSales,addCustomer,updateCustomer,deleteCustomer} from '../data/store.js';
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=n=>new Intl.NumberFormat('en-KE',{style:'currency',currency:'KES',maximumFractionDigits:0}).format(n);
export function renderCustomers(){const cs=getCustomers(),sales=getSales();const enriched=cs.map(c=>{const s=sales.filter(x=>x.customer===c.name);return{...c,orders:s.length,spent:s.reduce((a,x)=>a+Number(x.total||0),0)}});return `<div class="page-head"><div><h2>Customers</h2><p>Manage customer records and purchase activity.</p></div><button class="btn btn-primary" data-action="add-customer">+ Add Customer</button></div><div class="stats"><div class="stat"><div class="stat-top"><span>Customers</span><span class="stat-icon">👥</span></div><div class="stat-value">${cs.length}</div><div class="stat-note">Saved customer records</div></div><div class="stat"><div class="stat-top"><span>Customers With Orders</span><span class="stat-icon">🛍️</span></div><div class="stat-value">${enriched.filter(x=>x.orders).length}</div><div class="stat-note">Based on completed sales</div></div></div><section class="card"><div class="card-head"><h3>Customer Directory</h3><input id="customer-search" class="search-input" placeholder="Search name, phone or email"></div><div class="table-wrap"><table class="table"><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Orders</th><th>Total Spent</th><th>Actions</th></tr></thead><tbody id="customer-body">${rows(enriched)}</tbody></table></div></section>`;}
function rows(cs){return cs.length?cs.map(c=>`<tr><td><strong>${esc(c.name)}</strong><div class="muted">${esc(c.notes)}</div></td><td>${esc(c.phone)||'—'}</td><td>${esc(c.email)||'—'}</td><td>${c.orders}</td><td>${money(c.spent)}</td><td><div class="row-actions"><button class="text-btn" data-edit-customer="${esc(c.id)}">Edit</button><button class="danger-btn" data-delete-customer="${esc(c.id)}">Delete</button></div></td></tr>`).join(''):'<tr><td colspan="6"><div class="empty">No customers yet.</div></td></tr>';}
export function customerModal(customer=null){const c=customer||{};return `<div class="modal-backdrop" data-modal-close><div class="modal"><div class="modal-head"><h3>${customer?'Edit Customer':'Add Customer'}</h3><button class="icon-btn" data-modal-close>✕</button></div><form id="customer-form" class="modal-body"><input type="hidden" name="id" value="${esc(c.id)}"><div class="form-grid"><div class="form-group full"><label>Name *</label><input class="field" name="name" required value="${esc(c.name)}"></div><div class="form-group"><label>Phone</label><input class="field" name="phone" value="${esc(c.phone)}"></div><div class="form-group"><label>Email</label><input class="field" name="email" type="email" value="${esc(c.email)}"></div><div class="form-group full"><label>Notes</label><textarea class="field" name="notes" rows="3">${esc(c.notes)}</textarea></div></div><div class="modal-actions"><button type="button" class="btn" data-modal-close>Cancel</button><button class="btn btn-primary">${customer?'Save Changes':'Add Customer'}</button></div></form></div></div>`;}
export function saveCustomerFromForm(form){const d=new FormData(form),id=String(d.get('id')||'');const data={name:d.get('name'),phone:d.get('phone'),email:d.get('email'),notes:d.get('notes')};return id?updateCustomer(id,data):addCustomer(data);}
export function getCustomer(id){return getCustomers().find(c=>c.id===id)||null;}
export function searchCustomerRows(q){const sales=getSales();return getCustomers().map(c=>{const s=sales.filter(x=>x.customer===c.name);return{...c,orders:s.length,spent:s.reduce((a,x)=>a+Number(x.total||0),0)}}).filter(c=>[c.name,c.phone,c.email].some(v=>String(v).toLowerCase().includes(q))).map(c=>c);}
export {rows};
export const removeCustomer=deleteCustomer;