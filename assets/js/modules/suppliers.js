import {getSuppliers,addSupplier,updateSupplier,deleteSupplier,getPurchases} from '../data/store.js';
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=n=>new Intl.NumberFormat('en-KE',{style:'currency',currency:'KES',maximumFractionDigits:0}).format(n);
export function renderSuppliers(){const ss=getSuppliers(),ps=getPurchases();const rowsHtml=ss.length?ss.map(s=>{const total=ps.filter(p=>p.supplierId===s.id).reduce((a,p)=>a+Number(p.total||0),0);return `<tr><td><strong>${esc(s.name)}</strong><div class="muted">${esc(s.address)}</div></td><td>${esc(s.phone)||'—'}</td><td>${esc(s.email)||'—'}</td><td>${money(total)}</td><td><div class="row-actions"><button class="text-btn" data-edit-supplier="${esc(s.id)}">Edit</button><button class="danger-btn" data-delete-supplier="${esc(s.id)}">Delete</button></div></td></tr>`}).join(''):'<tr><td colspan="5"><div class="empty">No suppliers yet.</div></td></tr>';return `<div class="page-head"><div><h2>Suppliers</h2><p>Manage suppliers used for stock purchasing.</p></div><button class="btn btn-primary" data-action="add-supplier">+ Add Supplier</button></div><section class="card"><div class="card-head"><h3>Supplier Directory</h3></div><div class="table-wrap"><table class="table"><thead><tr><th>Supplier</th><th>Phone</th><th>Email</th><th>Purchases</th><th>Actions</th></tr></thead><tbody>${rowsHtml}</tbody></table></div></section>`;}
export function supplierModal(s=null){const x=s||{};return `<div class="modal-backdrop" data-modal-close><div class="modal"><div class="modal-head"><h3>${s?'Edit Supplier':'Add Supplier'}</h3><button class="icon-btn" data-modal-close>✕</button></div><form id="supplier-form" class="modal-body"><input type="hidden" name="id" value="${esc(x.id)}"><div class="form-grid"><div class="form-group full"><label>Name *</label><input class="field" name="name" required value="${esc(x.name)}"></div><div class="form-group"><label>Phone</label><input class="field" name="phone" value="${esc(x.phone)}"></div><div class="form-group"><label>Email</label><input class="field" name="email" type="email" value="${esc(x.email)}"></div><div class="form-group full"><label>Address</label><input class="field" name="address" value="${esc(x.address)}"></div><div class="form-group full"><label>Notes</label><textarea class="field" name="notes" rows="3">${esc(x.notes)}</textarea></div></div><div class="modal-actions"><button type="button" class="btn" data-modal-close>Cancel</button><button class="btn btn-primary">${s?'Save Changes':'Add Supplier'}</button></div></form></div></div>`;}
export function saveSupplierFromForm(form){const d=new FormData(form),id=String(d.get('id')||''),data={name:d.get('name'),phone:d.get('phone'),email:d.get('email'),address:d.get('address'),notes:d.get('notes')};return id?updateSupplier(id,data):addSupplier(data);}
export const getSupplier=id=>getSuppliers().find(x=>x.id===id)||null;export const removeSupplier=deleteSupplier;