import {getSettings,saveSettings} from '../data/store.js';
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export function renderSettings(){const s=getSettings();return `<div class="page-head"><div><h2>Settings</h2><p>Configure the business identity and receipt defaults.</p></div></div><section class="card"><div class="card-head"><h3>Business Settings</h3></div><form id="settings-form" class="modal-body"><div class="form-grid"><div class="form-group full"><label>Business Name</label><input class="field" name="businessName" value="${esc(s.businessName)}"></div><div class="form-group"><label>Phone</label><input class="field" name="phone" value="${esc(s.phone)}"></div><div class="form-group"><label>Email</label><input class="field" name="email" type="email" value="${esc(s.email)}"></div><div class="form-group full"><label>Address</label><input class="field" name="address" value="${esc(s.address)}"></div><div class="form-group"><label>Currency</label><select class="field" name="currency"><option value="KES" ${s.currency==='KES'?'selected':''}>KES — Kenyan Shilling</option><option value="USD" ${s.currency==='USD'?'selected':''}>USD — US Dollar</option><option value="ZAR" ${s.currency==='ZAR'?'selected':''}>ZAR — South African Rand</option></select></div><div class="form-group"><label>Default Tax Rate (%)</label><input class="field" name="taxRate" type="number" min="0" max="100" step="0.01" value="${Number(s.taxRate)||0}"></div><div class="form-group full"><label>Receipt Footer</label><textarea class="field" name="receiptFooter" rows="3">${esc(s.receiptFooter)}</textarea></div></div><div class="modal-actions"><button class="btn btn-primary">Save Settings</button></div></form></section>`;}
export function saveSettingsFromForm(form){const d=new FormData(form);return saveSettings(Object.fromEntries(d.entries()));}